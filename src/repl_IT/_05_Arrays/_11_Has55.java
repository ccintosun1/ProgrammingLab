package repl_IT._05_Arrays;public class _11_Has55 {    public static void main(String[] args) {        int[] nums = {1, 5, 5, 1};        for (int i = 0; i < nums.length; i++) {            int count = 0;            for (int j = 0; j < nums.length; j++) {                if (nums[j] == 5) {                    count++;                }            }            if (count == 0) {                System.out.println(false);                System.out.println("There is no 5 in this array");                System.exit(0);            } else if (nums[i+1] == 5) {                System.out.println(true);                System.exit(0);            } else if (nums[i + 1] != 5) {                System.out.println(false);                System.out.println("The 5s are not next to each other");                System.exit(0);            }        }    }}/*### Given an `int []` `nums`, print `true` if the array has a `5` that is also next to another `5`. Both conditions need to be met otherwise print `false`#### Note: The loop used in the given code is to dynamically create the int array. You don't need to fully understand this part yet, focus only on having access to an array and has some numbers.Main topics: arrays, primitive datatypes, concatenation, index, operators. loops, if statementExample:```Input:  [1, 5, 5, 1, 1]Output:  true```Example:```Input:  [1, 5, 4, 1, 5]Output:  false-> The 5s are not next to each other```Example:```Input:  [1, 4, 4, 1, 99]Output:  false-> There is no 5 in this array``` */